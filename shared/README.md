# Shared Modules

This directory contains definitions and constants shared between the frontend (React SPA) and backend (FastAPI):

* `constants/`: Shared business limits and configurations.
* `enums/`: Role names and status enums.
* `types/`: Common type definitions.
* `validators/`: Core input boundaries.
* `interfaces/`: API communication specifications.
